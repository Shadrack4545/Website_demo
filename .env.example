## Example environment variables (do NOT commit secrets)
# For frontend (Vite will pick up VITE_ vars at build time)
VITE_ML_API_URL=https://your-backend.example.com
VITE_ML_API_KEY=changeme

# For backend (Render env vars)
ML_API_KEY=changeme
MODEL_PATH=trained_model.pkl
